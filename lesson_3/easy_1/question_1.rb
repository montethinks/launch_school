# numbers = [1, 2, 2, 3]
# numbers.uniq

# puts numbers

Expected output is [1, 2, 2, 3]. 

uniq returns an array that removes all duplicates but it does not modify the original array unless uniq! is called. numbers is uneffected.