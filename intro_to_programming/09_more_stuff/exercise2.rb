# def execute(&block)
#   block
# end

# execute { puts "Hello from inside the execute method!" }

Nothing will print to the screen but the block "Hello from inside the execute method!" and it will return the Proc that was passed to it.